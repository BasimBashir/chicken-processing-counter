--extra-index-url https://download.pytorch.org/whl/cu126
torch
torchvision
opencv-python-headless
numpy
pillow
ultralytics
pandas
tqdm
seaborn
fastapi
uvicorn[standard]
python-multipart
pydantic-settings
python-dotenv
# TensorRT export deps — pre-installed so ultralytics auto-update never runs
onnx>=1.12.0,<2.0.0
onnxslim>=0.1.71
onnxruntime-gpu
# Pin <11: TensorRT 11.0 removed the weak-typing BuilderFlag family
# (BuilderFlag.FP16/INT8). Ultralytics 8.4.x's engine exporter still calls
# config.set_flag(trt.BuilderFlag.FP16), which AttributeErrors on TRT 11.
tensorrt>=10.0,<11
